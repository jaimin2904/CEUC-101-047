#include<stdio.h>
#include<conio.h>
int main()
  {
 clrscr();
     printf("value of int: %d\n",sizeof(int));
     printf("value of folat:%d\n",sizeof(float));
     printf("value of char:%d\n",sizeof(char));
     printf("value of double:%d",sizeof(double));
     getch();
     return 0;
      }
